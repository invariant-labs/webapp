import { colors, typography } from '@static/theme'
import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({

IdoHeader :{fontSize: "20px",lineHeight: "40px", fontFamily:"Mukta"}

}))

export default useStyles;