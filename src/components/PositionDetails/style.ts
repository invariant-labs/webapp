import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  wrapperContainer: {
    display: 'flex',
    width: 1058
  },
  positionDetails: {
    marginRight: 24,
    display: 'flex',
    width: 461
  }
}))

export default useStyles
