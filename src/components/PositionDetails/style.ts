import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  wrapperContainer: {
    display: 'flex',
    width: 1058
  },
  positionDetails: {
    paddingRight: 20,
    display: 'flex',
    alignItems: 'end'
  }
}))

export default useStyles
