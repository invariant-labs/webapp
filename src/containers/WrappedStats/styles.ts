import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  wrapper: {
    maxWidth: 1120
  },
  subheader: {},
  plotsRow: {},
  loading: {}
}))

export default useStyles
