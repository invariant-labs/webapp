import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  wrapper: {
    maxWidth: 918,
    minHeight: '100%'
  },
  loading: {
    width: 150,
    height: 150,
    margin: 'auto'
  }
}))

export default useStyles
